# simpleDiscordCryptRemastered
A remastered port of the original userscript created by An0 into a vencord-style userplugin.

!Currently in a semi-broken state, Key selector needs tweaks to the implementation, the message hook needs fixed, the file upload hook needs fixed, all the necessary logic for cryptography is essentially working already.
